api:
  build: .
  command: python runserver.py
  volumes:
   - .:/code
  links:
    - rethinkdb

rethinkdb:
  image: rethinkdb
  ports:
   - "28015:28015"
   - "8080:8080"
  volumes:
    - .:/data

nginx:
  image: nginx
  volumes:
   - frontend/static:/web
   - config/nginx:/etc/nginx/conf.d
  ports:
   - "80:80"
  links:
    - api